import { Typography } from 'antd';

const { Title } = Typography;

export const EventsList = () => {
  return (
    <div>
      <Title level={2}>Events</Title>
      {/* Add your events list content here */}
    </div>
  );
}; 